# Not needed for production (API is same-origin via Express static serving)
# Only needed if running client dev server separately
VITE_API_URL=http://localhost:3001

# Directory for state.json persistence (defaults to server/)
# STATE_DIR=./data

# Optional: shared secret for write auth (leave empty for open access)
# DRAFT_KEY=your-shared-secret-here
